const GetNotified = (): JSX.Element => {
  return (
    <div className="get-notified">
      <input type="email" placeholder="hello@example.com" />
      <button>Get notified</button>
    </div>
  );
};

export default GetNotified;
